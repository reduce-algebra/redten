coords '(t r th ph);
npnames(t);
complex rhob;
rho2 := rhob * cnj(rhob);
nulltet();
ias (l);
1;
-rho2/delta;
0;
-a*sin(th)^2;
ias (n);
delta/ (2*rho2);
1/2;
0;
-a*delta*sin(th)^2/(2*rho2);
ias(m);
i*a*sin(th)/(rhob*sqrt(2));
0;
-rho2/(rhob*sqrt(2));
-i*(r^2+a^2)*sin(th)/(rhob*sqrt(2));
mapfi(m);
;end;
